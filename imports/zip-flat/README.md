# Imported project bundle

This directory contains every file from `FindReadyExistingProjectsFullyWorkingandDeployed.zip` exactly as supplied. The archive contains 99 flattened files and no directory entries, so these files are preserved here for auditability rather than guessed into the canonical application paths. The deployable DRIFT application remains at its existing source paths.
